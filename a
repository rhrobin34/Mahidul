#include <bits/stdc++.h>
using namespace std;

int main() {
    int a, b;

    while (cin >> a >> b) {
        if (a == -1 && b == -1) break;
        int u = (b - a + 100) % 100;
        int d = (a - b + 100) % 100;
        cout << min(u, d) << endl;
    }
}


